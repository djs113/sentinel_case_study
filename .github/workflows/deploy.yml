name: Deploy to Raspberry Pi
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          cd ~/sentinel_case_study
          git checkout main
          git pull
          docker compose up --build -d